import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.vue.page.transition.test',
  appName: 'ionic-vue-page-transition-test',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
